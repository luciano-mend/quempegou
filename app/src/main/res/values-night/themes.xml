<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.QuemPegou" parent="Theme.Material3.Dark">
        <!-- Faz a barra de stats ficar transparente-->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowTranslucentStatus">true</item>

        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/azul_esverdeado</item>
        <item name="colorPrimaryVariant">@color/preto</item>
        <item name="colorOnPrimary">@color/preto</item>
    </style>
</resources>